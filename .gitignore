node_modules/
dist/
dist-agent/
.env
.env.*
!.env.example
*.local
*.pem
*.key
coverage/
# Testnet-only demo key material and run evidence — never commit.
.demo/

# Local-only working notes, specs and submission material. Never committed.
private/
